package p2;

import p1.EAM;

public class Fak_Rechner
{

	public static void main(String[] args)
	{
		System.out.println( Fak_Rechner.CalcFak(4));
	}
	public static int CalcFak (int p_number)
	{
		int l_storage = p_number;
		for(int i = p_number-1; i > 1; i--)
		{
			l_storage *= i;
		}
		return l_storage;	
			
	}	
	static long CalcFakRec(long n)
	{
	   if (n>1)
		   return(n*CalcFakRec(n-1)); 
	   else
		   return(1);
	}		
	
/*	public static int CalcFak (int p_number)
	{
		if(p_number > 1)
			return CalcFak(p_number * (p_number-1));
		else if(p_number == 0)
			return 0;
		else
			return 2;

			
			
	}	*/
	
}
